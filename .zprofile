# .zprofile is executed once when you log in to your shell session,
# before the first interactive shell is started.
#
# Set environment variables, aliases, and other shell settings that you
# want to be available in every shell session.
